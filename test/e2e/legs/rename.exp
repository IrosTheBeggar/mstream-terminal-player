#!/usr/bin/expect -f
set timeout 20
set env(TERM) xterm-256color
set env(MSTREAM_SETUP_LANG) en
set env(MSTREAM_NO_GRAPHICS) 1
set env(HOME) $::env(WIZHOME)
proc drain {secs} { expect -timeout $secs timeout {} }
spawn {*}$argv
exec stty rows 40 columns 100 < $spawn_out(slave,name)
expect { "Add a music folder" {} timeout { puts "SYNC-FAIL"; exit 2 } }
send "t"
drain 1
send "$::env(WIZMUSIC)\r"
drain 2
# Click the name chip of row 1 (col ~15, row 17, 1-based) and rename.
send "\x1b\[<0;15;16M"
drain 1
send "\x1b\[<0;15;16m"
drain 1
send "x"
drain 1
send "\r"
drain 2
send "\x03"
expect eof
exit 0
